apiVersion: v1
kind: Service
metadata:
  name: nostr-auth
  namespace: nostr-auth
  labels:
    app.kubernetes.io/name: nostr-auth
    app.kubernetes.io/part-of: nostr-auth
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nostr-auth
  ports:
    - name: http
      port: 80
      targetPort: http
